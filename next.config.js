import "./src/env.js";

/** @type {import("next").NextConfig} */
const config = {
  images: {
    domains: [
      'pjrjxbdononaezaz.public.blob.vercel-storage.com',
      "utfs.io"
    ],
  },
};

export default config;
